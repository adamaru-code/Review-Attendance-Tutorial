<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all',
                               'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application',
                               'data-turbolinks-track': 'reload' %>
    <!-- パーシャル（partial）機能を使って HTML shim がこの1行で収まる。 旧式のInternet Explorerに対応するための HTML shim を　app/views/layouts/_shim.html.erb へ要移動記述-->
    <%= render 'layouts/shim' %>
  </head>
  <body>
    <!-- パーシャル（partial）機能を使ってheaderの内容がこの1行で収まる。app/views/layouts/_header.html.erb へ要移動記述-->
    <%= render 'layouts/header' %>
    <div class="container">
      <%= yield %>
      <%= debug(params) if Rails.env.development? %> <!-- 開発環境（development）・テスト環境（test）・本番環境（production） -->
    </div>
  </body>
</html>